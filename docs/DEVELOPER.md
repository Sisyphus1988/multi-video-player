# 起居注

## 2026-05-08 监国模式战果（第二轮深度审查修复）

- 核心目标：深度审查后修复 6 项问题（提交 `55eb08c`）。
- 已完成：`reIndexCards` 事件监听器正确移除 — `createVideoCard` 中将监听器赋值给 `video._onMeta` 等属性，reIndexCards 通过 stored references 移除旧监听器再绑定新的，不再泄漏。
- 已完成：`removeVideo` 改用 `querySelector(.video-card[data-index])` 精确定位卡片，不再依赖 children 顺序。
- 已完成：单击视频直接播放/暂停，双击进入全屏，去掉 250ms 延迟，消除全局 clickTimer 状态冲突。
- 已完成：取消静音时恢复上次音量（lastVolume），不再强制为 1。
- 已完成：删除未使用的 `renderAllCards` 死代码；`escapeHtml` 补充单引号转义。
- 已完成：`timeupdate`/`progress` 事件用 `requestAnimationFrame` 节流，减少高频 DOM 操作。
- 已完成：`loadVideoFiles` 用 `DocumentFragment` 批量插入，减少重排。
- 验证闭环：JS 语法解析通过；`git diff --check` 通过；工作区干净。
- 遗留暗雷：rAF 节流需在浏览器中验证进度条/时间显示是否仍然流畅。

## 2026-05-08 监国模式战果（第一轮）

### 第一轮：功能扩展（提交 `01e8e42`）

- 核心目标：优化本地多视频播放器。
- 已完成：视频导入改为追加模式，不再覆盖已有视频。
- 已完成：每个视频卡片新增删除按钮（✕），支持单个移除并释放内存。
- 已完成：双击视频进入/退出全屏；新增画中画(PiP)悬浮小窗按钮。
- 已完成：每个视频独立倍速控制（0.25x~4x 橙色滑块）。
- 已完成：工具栏新增循环播放开关（🔁 循环/循环中）。
- 已完成：进度条显示缓冲进度，hover 时加粗。
- 提交记录：`01e8e42 feat: 追加导入、删除、全屏、画中画、倍速、循环、进度条美化`。

### 第一轮：Bug 修复与体验优化（提交 `3793644`）

- 核心目标：修复审查发现的 7 项问题。
- 已完成：删除/追加视频不再中断正在播放的视频，改为局部 DOM 更新 + reIndexCards。
- 已完成：formatTime 支持小时显示（H:MM:SS），长视频不再显示为 `90:00`。
- 已完成：进度条改为三段式渐变（已播放蓝 / 缓冲灰蓝 / 未缓冲深色），JS 直接设置容器背景。
- 已完成：补充 Firefox `::-moz-range-track/thumb` 样式，三种滑块在 Firefox 下风格统一。
- 已完成：追加导入后显示「已选择 X 个视频（+N 新增）」反馈。
- 已完成：escapeHtml 改为纯字符串替换，不再创建 DOM 元素。
- 已完成：视频名称允许两行显示，超长文件名不再完全截断。
- 提交记录：`3793644 fix: 修复7项问题 — 播放中断、时间格式、进度条、Firefox兼容、追加反馈、escapeHtml、名称溢出`。
- 验证闭环：JS 语法解析通过；`git diff --check` 通过；工作区干净。
- 遗留暗雷：未做真实浏览器交互验证；reIndexCards 用 cloneNode 重绑事件，需回归测试删除后各控件是否正常。

## 2026-05-07 勤政日志

- 项目判定：轻型单文件前端应用，入口为 `video-player.html`。
- 今日改进：修复窄屏下视频网格 `400px` 最小列宽导致的横向溢出；控制条允许换行，时间与音量控件保持稳定宽度。
- 提交记录：`779a717 fix: 修复窄屏视频布局溢出`。
- 验证闭环：已用 Node 解析内联脚本语法；已检查响应式样式命中；暂存区仅包含本次修复后提交。
- 遗留暗雷：接手前 `video-player.html` 已存在未提交改动，包含文件输入、XSS 防护、对象 URL 释放、播放控制重构等内容，尚未由本轮提交纳入。
- 明日首要任务：复核现有未提交改动是否符合业务初衷，必要时拆分验证并提交。

## 2026-05-07 监国模式战果

- 核心目标：继续优化本地多视频播放器。
- 已完成：复核并收编遗留的安全与资源管理改动，补强文件名转义、对象 URL 回收、播放失败状态回退和不可播放提示。
- 已完成：新增“清空列表”操作，清空时释放对象 URL、重置文件输入，并在无视频时禁用批量控制按钮。
- 已完成：为视频元素设置 `preload="metadata"`，降低多视频场景初始资源压力。
- 已完成：补全按钮类型、文件选择标签、计数播报、单视频播放/静音/进度/音量控件的辅助标签，并修正音量为 0 时取消静音仍无声的问题。
- 提交记录：`098f1ab fix: 强化视频加载状态处理`；`f448cb5 feat: 添加视频列表清空操作`；`cb563e1 fix: 改善播放器控件可访问性`。
- 验证闭环：内联脚本通过 Node 语法解析；`git diff --check` 通过；工作区干净；未发现 `.bak`、`.tmp` 或 `test_` 临时残留。
- 遗留暗雷：未做真实浏览器交互录制验证，本机未检测到已安装 Playwright。
- 明日首要任务：如继续优化，优先补充真实浏览器 smoke test 或做一次手动播放/清空/重选同文件回归。

## 2026-05-07 文件夹导入优化

- 用户目标：支持直接多选视频文件，或打开某个文件夹下所有视频。
- 已完成：将“选择视频”明确为“选择视频文件”，保留多选文件能力。
- 已完成：新增“选择文件夹”入口，通过目录选择自动读取文件夹及子文件夹内文件，并按视频 MIME 类型或常见视频扩展名过滤。
- 已完成：文件夹导入时按相对路径排序并显示相对路径，便于区分不同子目录下的同名视频。
- 验证闭环：内联脚本语法解析通过；最小 DOM 桩初始化通过；`git diff --check` 通过。
- 提交记录：`dbf9080 feat: 支持选择文件夹导入视频`。
- 遗留暗雷：文件夹选择依赖浏览器的 `webkitdirectory` 能力，主流 Chromium/Edge 支持；浏览器安全限制不允许网页无确认地扫描任意本地路径。

## 2026-05-07 拖拽导入优化

- 用户目标：继续优化本地多视频播放器。
- 已完成：支持将视频文件直接拖入页面导入，自动复用现有视频过滤、排序、渲染流程。
- 已完成：支持在 Chromium/Edge 中拖入文件夹并递归读取其中视频；普通文件拖拽保留标准 `DataTransfer.files` fallback。
- 已完成：拖拽过程中显示全屏投放提示，松手后自动隐藏。
- 验证闭环：内联脚本语法解析通过；最小 DOM 桩 drop smoke test 通过；`git diff --check` 通过。
- 提交记录：`9a747bb feat: 支持拖拽导入视频`。
- 遗留暗雷：文件夹拖拽同样依赖 Chromium 系浏览器的 `webkitGetAsEntry` 能力；真实浏览器手动拖入文件夹仍建议补一次回归。
